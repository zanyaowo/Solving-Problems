#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
    stringstream c();
    string input;
    getline(cin, input);
    vector<int> a(20);
    cout << input;
    return 0;
}

